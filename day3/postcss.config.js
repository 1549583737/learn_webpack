module.exports = {
    plugins: [
        [
            'postcss-preset-env',
            {
                // Options 这里是postcss-preset-env的配置项
                // browsers: 'last 10 versions'
            }
        ]
    ]
}